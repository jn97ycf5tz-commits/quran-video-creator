# Quran Video Creator - Python Dependencies
# Install all dependencies: pip install -r requirements.txt

# Core Video Processing
moviepy[optional]>=1.0.3         # Video editing and composition
pillow>=9.0.0                     # Image processing and text rendering
numpy>=1.21.0                     # Numerical operations

# Audio Processing
edge-tts>=6.1.0                   # Text-to-speech for translations

# API Integrations
requests>=2.28.0                  # HTTP requests for APIs
openai>=1.0.0                     # OpenAI API client (optional)

# Arabic Text Support
arabic-reshaper>=3.0.0            # Proper Arabic text shaping
python-bidi>=0.4.2                # Bidirectional text support (RTL)

# Configuration Management
python-dotenv>=1.0.0              # Environment variable management

# Additional Dependencies (installed automatically with moviepy[optional])
# - imageio
# - imageio-ffmpeg
# - decorator
# - proglog
# - tqdm

# System Requirements (not in pip):
# - FFmpeg (required for video encoding)
#   Windows: choco install ffmpeg
#   macOS: brew install ffmpeg
#   Linux: sudo apt install ffmpeg
